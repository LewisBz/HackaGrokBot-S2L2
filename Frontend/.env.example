# false (default) = llama al backend real POST /api/ruta (NLP + OSRM + reportes)
# true = respuesta mock local sin backend
VITE_USE_MOCK=false

# Opcional: URL absoluta si no usas el proxy de Vite
# VITE_API_URL=http://127.0.0.1:8000/api/ruta
